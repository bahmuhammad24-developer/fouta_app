# This file is generated by Flutter.
# You are encouraged to leverage the following APIs when customizing your Podfile.
#
# A newer API is available now that is more modular and allows for more customization.
# See: https://docs.flutter.dev/deployment/ios#podfile
#
# For more details, see: https://guides.cocoapods.org/using/the-podfile.html

# Use the modern Flutter integration.
flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_plugin_pods 'Runner', {
    'permission_handler' => { :ios => 'Permission-Camera' }
    # Add other plugin-specific configurations here.
  }
end

post_install do |installer|
  # This is the modern entry point for Flutter's post-install logic.
  flutter_post_install(installer)

  # As per our project history, a custom script was added here to remove a
  # problematic compiler flag. That logic should be preserved within this block.
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings.delete('GCC_WARN_UNUSED_VARIABLE')
    end
  end
end